% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools.R
\name{tool_ROS}
\alias{tool_ROS}
\title{Calculate rate of spread (ROS) for a single set of conditions}
\usage{
tool_ROS(
  season = "summer",
  density = "moderate",
  stand = "pine",
  FSG = 6,
  SFC = 2,
  CBD = 0.16,
  smooth_CFO = FALSE,
  model_conpyro = 11,
  model_SROS = 1,
  model_CROS = 1,
  WS = 20,
  FFMC = 91,
  DMC = 70,
  CF_thresh = 0.5,
  ROS_output = "integrated"
)
}
\arguments{
\item{season}{One of \code{spring}, \code{sp-su}, \code{summer}, or \code{fall}.}

\item{density}{One of \code{light}, \code{moderate}, or \code{dense}.}

\item{stand}{One of \code{pine}, \code{spruce}, \code{Douglas-fir}, \code{deciduous}, or
\code{mixedwood}.}

\item{FSG}{A numeric value between \code{0.5} and \code{20} (inclusive). Fuel strata
gap in metres. The vertical distance between the top of the surface fuels
and the lower limit of the canopy fuels. Analogous to crown base height
(CBH) in the absence of mid-story ladder fuels.}

\item{SFC}{A numeric value between \code{0.1} and \code{6} (inclusive). Surface fuel
consumption in kg/m^2. May be estimated using \code{\link[=tool_SFC_FBP]{tool_SFC_FBP()}} or
\code{\link[=tool_SFC_deGroot]{tool_SFC_deGroot()}}.}

\item{CBD}{A numeric value between \code{0.01} and \code{0.8} (inclusive). Crown bulk
density in kg/m^3.}

\item{smooth_CFO}{Defines whether crown fire initiation is modeled as a
smooth transition (\code{TRUE}) or an instantaneous occurrence (\code{FALSE}).}

\item{model_conpyro}{Choose one of \code{7}, \code{8}, \code{10}, or \code{11}. The ConPyro model
form used for calculations (see Perrakis et al., 2023). Models \code{7} and \code{10}
use FFMC-based fine fuel moisture content (MCFFMC) while models \code{8} and
\code{11} use stand-adjusted moisture content (MCSA). Default is \code{11}.}

\item{model_SROS}{Choose one of \code{1}, \code{2}, \code{3}, or \code{4}. The surface fire ROS
model used for calculations.
* \code{1}: Aggregated FBPS surf. V4 (default)
* \code{2}: D-1 FBPS
* \code{3}: C-6 (surface only) FBPS
* \code{4}: IsaSFC}

\item{model_CROS}{Choose one of \code{1} or \code{2}. The crown fire ROS model used for
calculations.
* \code{1}: Adapted Cruz, Alexander, & Wakimoto (2005): WS, MC, CBD (default)
* \code{2}: Adapted Cruz & Alexander (2019): WS only}

\item{WS}{A single integer between \code{0} and \code{60} (inclusive). Wind speed in
km/h}

\item{FFMC}{A numeric value between 80 and 99 (inclusive). The Fine Fuel
Moisture Code (FFMC) as per the Canadian Forest Fire Weather Index System.}

\item{DMC}{A numeric value between 5 and 200 (inclusive). The Duff Moisture
Code (DMC) as per the Canadian Forest Fire Weather Index (FWI) System.}

\item{CF_thresh}{A numeric value between \code{0} and \code{1} (inclusive). Defines the
pCFO threshold at which crown fire occurs.}

\item{ROS_output}{A character vector to control ROS output. Choose any of the
following:
\itemize{
\item \code{SROS}: Surface fire rate of spread.
\item \code{CROS_P}: Passive crown fire rate of spread.
\item \code{CROS_A}: Active crown fire rate of spread.
\item \code{integrated}: Complete composite rate of spread.
}}
}
\value{
A list of length 4 consisting of numeric values named:
\itemize{
\item \code{MCFFMC}
\item \code{MCSA}
\item \verb{Crown Fire Occurrence Probability (pCFO)}
\item \verb{Integrated Rate of Spread (m/min)}
}
}
\description{
\code{tool_ROS()} calculates rate of spread for a single set of fuel, wind speed,
and stand structure observations using the Canadian Conifer Pyrometrics
(ConPyro) model system. See Perrakis et al. (2023) for details.
}
\examples{
# TODO
}
